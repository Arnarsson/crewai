version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: ../docker/dev/backend.Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    env_file:
      - ./backend/.env
